apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-b
  labels:
    app: aceest-fitness
    variant: b
spec:
  type: NodePort
  selector:
    app: aceest-fitness
    variant: b
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30084
